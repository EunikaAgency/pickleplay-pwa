# pickleballers-api

Hono + MongoDB API for PickleBallers (port 9002).

- 🗺️ **The file-map is [FILEMAP.md](FILEMAP.md)** — skim it first before
  grepping/scanning `src/`.
- Deep architecture, the add-a-feature checklist, and the `/lists` rule live in
  [CLAUDE.md](CLAUDE.md).
- Repo-wide conventions for all agents live in [../AGENTS.md](../AGENTS.md).

```sh
npm install
npm run dev      # tsx watch src/index.ts (port 9002)
```
