# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Commands

```sh
npm run dev      # Start Vite dev server on port 9000
npm run build    # TypeScript check + Vite build
npm run lint     # ESLint all source
npm run preview  # Preview production build
```

The app runs at `http://localhost:9000`. API requests to `/api/*` are proxied to `http://localhost:9002` (the sibling Hono/MongoDB API at `/var/public/pickleplay/api`).

## Architecture

**Mobile-first React PWA** for finding pickleball games, courts, clubs, and players.

### Stack
- React 19 + TypeScript 6 + Vite 8
- Tailwind CSS v4 (via `@tailwindcss/vite` plugin)
- Zustand 5 — used for auth/session state (`shared/lib/authStore.ts`); everything else is local `useState` in `App.tsx` and per screen
- Leaflet / react-leaflet for map views
- Google Material Symbols Outlined for icons (loaded as a Google Font; wrapped by `shared/components/ui/Icon`)
- vite-plugin-pwa with Workbox (map tile runtime caching) + custom `pwaUpdate.ts` for service-worker auto-update
- Fonts: Fredoka (headings), Nunito Sans (body) via Google Fonts
- PM2 ecosystem config for production process management

### Navigation
Custom screen-stack system in `App.tsx`. Each screen is a discriminated union `Screen` with an `id` and optional `params`. The `navigate()` function pushes the current screen onto a history stack; `goBack()` pops it. Five tab screens (home, nearby, games, clubs, profile) use `activeTab` state. Non-tab screens show a back arrow rendered by the screen itself.

The cold-start entry point is **LandingScreen** (a marketing/welcome surface). Tapping "Get Started" or "Sign In" navigates to **LoginScreen**. Logout returns to LandingScreen, not LoginScreen.

### Architecture — feature-based vertical slices

Code is organised by **feature**, not by technical layer. Every feature owns its screens and any feature-specific UI (filter sheets, forms). Cross-feature UI primitives, hooks, types, and the global stylesheet live in `shared/`.

```
src/
  App.tsx                          # Composition root: screen-stack nav + chrome wiring
  main.tsx                         # Entry point
  pwaUpdate.ts                     # Service-worker auto-update registration

  features/
    auth/                          # Unauthenticated/cold-start flow
      LandingScreen.tsx
      LoginScreen.tsx
      OnboardingScreen.tsx
    home/
      HomeScreen.tsx
    games/
      GamesScreen.tsx
      GameDetailsScreen.tsx
      CreateGameScreen.tsx
      InvitePlayersScreen.tsx
      GameFilterSheet.tsx
    venues/                        # "Nearby" tab + court detail (matches web/'s naming)
      NearbyScreen.tsx
      CourtDetailsScreen.tsx
      NearbyFilterSheet.tsx
    clubs/
      ClubsScreen.tsx
      ClubDetailsScreen.tsx
      CreateClubScreen.tsx
    profile/                       # User dashboard (analog of web/'s `my/`)
      ProfileScreen.tsx
      EditProfileScreen.tsx
      SettingsScreen.tsx
      NotificationsScreen.tsx
    search/
      SearchScreen.tsx

  shared/
    components/
      layout/                      # TabBar (mobile), Sidebar (desktop)
      ui/                          # Icon, Avatar, Button, Card, Chip, BottomSheet,
                                   # CompletionScreen, CourtIllustration, DemoBranch,
                                   # DemoStateControl, DuprExplainerSheet, EmptyState,
                                   # ErrorState, GameRow, InstallPrompt, LoadingSkeleton,
                                   # OfflineBanner, ProgressBar, ScreenHeader, Segmented,
                                   # Toast
      forms/                       # FormField, FormSelect, FormTierPicker
    hooks/
      useForm.ts                   # Generic form state + validation helper
      usePrefersReducedMotion.ts   # Respects OS reduce-motion preference
      useTheme.ts                  # Theme application
    lib/
      types.ts                     # Court, User, Game, Club, Message interfaces
      authStore.ts                 # Zustand auth/session store (user, login/logout/restore)
      api.ts                       # API client: auth (+token storage, toAppUser) + venues/courts
      venueDisplay.ts              # Venue formatters: price/location/tags/amenities/mapsUrl
      permissions.ts               # Roles → permissions, AppUser, userHasPermission, firstNameOf
      navigation.ts                # Screen union, ScreenId, tabScreens, Navigate
      initials.ts                  # getInitials(name) helper
      demoState.tsx                # DemoStateProvider + useDemoState
      skillTiers.ts                # Skill-tier definitions and helpers
    styles/
      index.css                    # Tailwind directives + global styles + design tokens
```

#### Adding a new screen or feature — checklist

1. **Pick the slice.** If it belongs to an existing feature, add `<NewName>Screen.tsx` inside `src/features/<feature>/`. Otherwise create `src/features/<new-feature>/` and put it there.
2. **Mount it in [src/App.tsx](src/App.tsx)** — the composition root. Add the screen to the `Screen` discriminated union, import it as `'./features/<feature>/<NewName>Screen'`, and add the `case` to `renderScreen()`.
3. **Cross-feature imports go via the shared layer:**
   - UI primitives: `'../../shared/components/ui/Icon'`, `'../../shared/components/ui/Button'`, etc.
   - Layout chrome: `'../../shared/components/layout/TabBar'`.
   - Forms: `'../../shared/components/forms/FormField'`.
   - Hooks: `'../../shared/hooks/useForm'`.
   - Types: `'../../shared/lib/types'`.
4. **Feature-specific UI stays in the feature.** Filter sheets, feature-only components, and feature-scoped hooks live next to the screens that use them and import as `'./<Sibling>'`.
5. **No deep relative paths** like `'../../../...'`. Three `..`s means a layer should be crossed via `shared/`.
6. **Don't reintroduce `src/screens/`, `src/components/`, `src/hooks/`, or `src/lib/`** at the top level — those flat dirs were removed during the vertical-slice migration.
7. **`npm run build` must stay clean.** Run it after structural changes to catch broken imports.

### Data types
Defined in [`src/shared/lib/types.ts`](src/shared/lib/types.ts): `Court`, `User`, `Game`, `Club`, `Message`. These represent the domain model used across screens. Most screen data is still inline/demo content. The wired-to-API exceptions are **auth** (see the next section) and **venues/courts** (the "Courts" tab): `NearbyScreen` and `CourtDetailsScreen` fetch from `/api/v1/venues` via `api.ts` (`listVenues`/`getVenue` → `ApiVenue`/`ApiVenueDetail`), with formatting in `venueDisplay.ts`. Real venue data is sparse (lat/lng, ratings, amenities are often null), so both screens degrade gracefully and own their loading/error/empty states (DemoBranch still overrides for reviewer modes). The list **paginates** with the API's cursor — 20 per page via a "Load more" button (`listVenues` returns `{ items, cursor }`; pass `cursor` for the next page). Venue **images** come from the API's media-derived `image` field (list + detail); only ~20 seeded venues have one, so cards fall back to a gradient. Court detail screens are mounted with `key={id}` so they remount per venue. The court-detail **location card** renders a real Leaflet map at the venue's coords (`venueCoords`), and the **"Games here"** list is wired to `listGames({ venueId })` — both formerly demo.

### Auth & the current user
- **`shared/lib/api.ts`** is the auth API client: `login`/`logout`/`fetchCurrentUser` hit `POST|GET /api/v1/auth/*`, tokens are stored in `localStorage` (`pb-access-token`/`pb-refresh-token`), and `toAppUser()` maps the API payload onto the app's `AppUser`. Base URL is relative in dev (Vite proxies `/api` → `:9002`); set `VITE_API_BASE_URL` for prod.
- **`shared/lib/authStore.ts`** is a Zustand store holding `user` + `isLoggedIn` and the `login`/`logout`/`restore` actions. **Screens read the current user directly** via `useAuthStore((s) => s.user)` — do not pass the user through props. `App.tsx` calls `restore()` on mount and still owns navigation + permission gating (`userHasPermission(currentUser, …)`).
- Identity fields (name, avatar, skill/DUPR, bio) render from the real user; profile **stats** (win rate, games, streak, achievements) are still demo data because the API doesn't expose them. `EditProfileScreen` prefills from the user and **saves to the account** via `authStore.updateProfile()` → `PATCH /me` (name/bio/skill tier; the picker tier maps to a numeric `skillLevel` via `duprForTier`). The free-text **Location** field is not persisted yet — the user model only has a `homeCityId` ref, not a free string.
- **Onboarding is remembered on the account.** The user model carries a `hasOnboarded` flag (exposed on `/me`). `OnboardingScreen` calls `authStore.completeOnboarding()` (→ `PATCH /me { hasOnboarded: true, … }`, best-effort) when the user finishes/skips, saving their skill tier too. `App.tsx` shows onboarding after login only when `!user.hasOnboarded`, so returning users are never re-onboarded (across devices/reloads).

### Key patterns
- Screens receive `onNavigate` for navigation and `onBack` for going back. A few also accept entity IDs as props (e.g. `gameId`, `courtId`, `clubId`).
- The `Icon` component wraps Material Symbols Outlined, supporting `filled`, `weight`, and `size` props via CSS `fontVariationSettings`.
- Tailwind classnames are inline; no CSS modules or styled-components.
- The TabBar renders five fixed bottom tabs and also receives an `onCreate` handler — the create action is inlined into the tab bar (no separate FAB). It is shown only on tab screens when logged in.
- Chrome visibility (TabBar only) is controlled by a `hideChrome` allowlist in App.tsx (`['landing', 'onboarding', 'login']`).
- Filter UIs are bottom sheets opened over the current screen via the shared `BottomSheet` primitive — they do not push onto the navigation stack.
- Loading states use `LoadingSkeleton` (layout-shaped placeholders), not a centered spinner.
- The `DemoStateProvider` wraps the app so any reviewer can switch between normal/empty/loading/error/offline modes at runtime via the `DemoStateControl` widget. The `OfflineBanner` shows when offline mode is selected or the browser reports offline.

### Reference assets
The visual source of truth for the latest mobile redesign lives under [`app/Redesign/`](./Redesign/): a JSX prototype (`app.jsx`), `ios-frame.jsx` wrapper, `screens-tabs.jsx`, `screens-detail.jsx`, shared `components.jsx`, `icons.jsx`, a `tweaks-panel.jsx` for live design tweaking, a self-contained `PickleBallers.html` showcase, and source PNG illustrations. Match against this folder when polishing the implementation.

---

## Keeping the public roadmap current

The product's public progress page lives at **https://pickleballer.eunika.xyz/roadmap** and is rendered from `/var/public/pickleplay/web/src/features/marketing/RoadmapPage.jsx` in the sibling web/ project. **Whenever you finish a meaningful task in this PWA — new screen, removed screen, big refactor, lifecycle change — you must update that roadmap as part of the same work**:

1. Edit `/var/public/pickleplay/web/src/features/marketing/RoadmapPage.jsx`.
2. Update the `Last updated:` string in the hero (~line 81) to today's date.
3. Prepend a new entry at the top of the Change Log array (~line 970): `{ date: 'YYYY-MM-DD', change: '…' }`.
4. If the task touches the screen inventory (added/removed/renamed screens or sheets), update the relevant Section tables (Existing Screens, Screen list — before vs after, etc.) so the roadmap stays truthful.
5. If a phase status changes (e.g. Phase 2 going from `status="active"` to `status="done"`), update the relevant `TimelineItem`.
6. The web project is in the same git remote as this one (parent monorepo `EunikaAgency/pickleplay-pwa`), so a single commit at the parent level can include both your app/ change and the roadmap update.

Skipping the roadmap update is treated like skipping a test: the work isn't done.

> Editing the web-repo roadmap from `app/` is the **single sanctioned exception**
> to the frontend-isolation rule (`../AGENTS.md` → "Stay in your lane") — only
> `web` can render it. Don't touch any other `web` file from an app task.

---

## Keeping the API endpoint catalogue (`/lists`) current

If your work adds, removes, or changes an API route (in the sibling `api/`
repo), you must also update the endpoint catalogue at
**https://pickleballer-api.eunika.xyz/lists** so it never drifts from what the
API exposes. Source: `api/src/features/root/root.controller.ts` →
`listEndpoints()`; see `api/CLAUDE.md` for the full checklist. Skipping it is
treated like skipping a test: the work isn't done.

---

## Keeping the file-map (`FILEMAP.md`) current

[`FILEMAP.md`](FILEMAP.md) is this repo's file-map — skim it before scanning
`src/`. **Whenever you add, remove, rename, or move a file/folder, or change a
file's primary responsibility, update it in the same change** (the directory
tree comments, the key-modules list, and the "Where to look first" table). Don't
touch it for logic-only edits that don't change what a file is for. Full rule +
rationale: [`../AGENTS.md`](../AGENTS.md) → "Keep the file-map (`FILEMAP.md`)
current". Skipping it is treated like skipping a test: the work isn't done.

---

## Change History

| Date | Change |
|---|---|
| 2026-06-26 | **The owner Members tab now lists actual MEMBERS (players who joined the membership), not everyone who booked/played — and the player "Join Membership" finally persists server-side.** Previously the player-facing "Join Membership" (`features/venues/CourtDetailsScreen.tsx` → `MembershipSheet`) only wrote `localStorage` (membership.ts: "no backend yet"), while the owner's `features/owner/tabs/MembersTab.tsx` rolled up *everyone who'd booked or played* from the bookings inbox and called them "members" — so subscribers were invisible and bookers were mislabelled. Now: **(1)** CourtDetailsScreen joins/cancels via the new self-service endpoints (`joinVenueMembership`/`leaveVenueMembership` in `api.ts` → `POST|DELETE /venues/:id/membership`), seeding its state from the venue detail's new `viewerMembershipTier` (+ existing `viewerIsMember`); the device-local `readMemberships`/`writeMembership`/`MEMBERSHIP_KEY` are gone. Guests tapping "Join Membership" are routed to `login` (the membership requires an account). **(2)** `MembersTab` was reworked to list the `VenueMember` set (`listVenueMembers`) — each row shows the player's plan/tier + "member since", with a **Remove** (revoke) button; the empty/description copy now talks about joining the membership ("Booking a court or playing a game doesn't make someone a member"); stats are Members count + Member rate (`memberDiscountPercent`). **"Add member"** no longer routes to the front desk — it opens a `BottomSheet` picker of past players (still derived from `getVenueBookings`, but now only a *candidate pool*, not the member list) so the owner can grant a membership (`addVenueMember`). Members self-joined this way get member pricing automatically. **API change (sibling `api/`):** new self-service `POST|DELETE /api/v1/venues/:id/membership` (the player joins/cancels their OWN — counterpart to the owner-managed `/members`, which stays owner/staff-gated; recorded as the same `VenueMember`), and `getVenue` now also returns `viewerMembershipTier`. Both on `/lists`. **No new permission** — self-join is `requireAuth` self-scoped (mirrors how a player joins a club). Verified end-to-end via curl (join → `viewerIsMember`/`viewerMembershipTier` reflect on the player's venue fetch → appears in the owner Members list → cancel → both clear + drops from the owner list; unauth join 401). Build + scoped lint clean (no new errors). App + API FILEMAP updated. |
| 2026-06-26 | **The game lobby's Share button works now (it was a dead control).** `features/games/GameDetailsScreen.tsx` — the top-right Share icon in the lobby hero had no `onClick`; it now calls a new `shareGame` that shares a deep link to the lobby (`…/games/<id>`, the existing `game-details` deep-link path) — native share sheet where available, else copy-to-clipboard + a "Game link copied" `Toast` (added the `Toast` import + state). Mirrors the club invite `shareInvite`; reuses `gameTitle` for the share text. The neighbouring Save/heart button stays dead (out of scope). No new permission/API/route/`/lists` change; FILEMAP unchanged (logic-only). Build clean (no new TS/lint errors; pre-existing working-tree errors elsewhere untouched). |
| 2026-06-24 | **Tapping a club feed post now opens it as a Facebook-style single-post page (permalink), replacing the inline comment expansion.** New **`features/clubs/ClubPostScreen.tsx`** (screen `club-post`, path `/clubs/:id/posts/:postId`) — shows the post (author/body/photos/like) + every comment + a sticky composer, and owns commenting and comment edit/delete; it's live (subscribes to the club SSE stream, debounced refetch) and reuses the image lightbox. In `features/clubs/ClubDetailsScreen.tsx` the feed post body **and** the comment button now `onNavigate('club-post', …)`; the whole inline thread (expand/collapse, lazy reply load, per-reply ⋯/edit, inline comment composer) and its state/handlers (`openPostId`/`repliesByPost`/`commentDraft`/`toggleComments`/`submitComment`/`openPostIdRef`) were removed — the feed is now a clean list of post previews (Like inline, comment count, ⋯ edit/delete on the post, photo→lightbox). New `getClubPost` client (`GET /:id/posts/:postId` → the post). The club-detail **active tab is remembered per club** (`clubTabMemory`) so returning from a post lands back on Feed, not About. Wired `club-post` in `navigation.ts` (union/path/parser/`deepLinkParent`→clubs) + `App.tsx` (render case, `tabForScreen`→clubs); **no SCREEN_PERMISSIONS** (public read like `club-details`). No new permission/API/route/`/lists` change (the single-post + replies endpoints already existed). FILEMAP updated. Verified via Playwright (9/9): tap a feed post → `/clubs/:id/posts/:postId`, post + seeded comment shown, post a comment → appears, like works, back returns to the **Feed** tab, and a cold deep-link to the permalink renders. |
| 2026-06-24 | **Expired sessions now sign out cleanly instead of looping on 401s.** When a token refresh fails for good (refresh token expired/revoked), `shared/lib/api.ts` clears the tokens and fires a new `onSessionExpired` notifier that `shared/lib/authStore.ts` subscribes to — flipping the store to guest so the notification-badge poll + realtime stream stop, instead of leaving a "zombie" logged-in state that retried authed requests (`/notifications/unread-count`, `/clubs/:id/feed`, …) forever and spammed `401`s in the console. Cold-start expiry was already handled by `restore()`; this covers the **mid-session** case. Also **coalesced concurrent refreshes** — `refreshSession` now shares one in-flight promise (`doRefreshSession`), so simultaneous 401s (e.g. the badge poll + a feed load on the same tick) no longer each rotate the refresh token, where the 2nd would 401 on the now-stale token and spuriously tear down a session that was actually just renewed. One-directional wiring (`authStore → api`), so no circular import. No new screen/permission/API/route/`/lists`; FILEMAP unchanged (logic-only). Verified via Playwright: dead session (both tokens invalid) → tokens cleared + UI flips to guest + **exactly one** refresh attempt + poll stops (no loop); happy path (access expired, refresh valid) → silent refresh, stays logged in, token rotated. |
| 2026-06-24 | **v2.1 top-nav now renders identically on every tab — fixed the Map/Tournaments header's missing side padding (and the back-chip border/shadow).** The shared `V2TopNav` is one component, but two of its rules were being clobbered per-tab via CSS specificity ties that the per-screen scopes won by source order: **(1) side padding** — the inner row `.pb-v2 .v2c-inner { padding: 0 16px }` (0,2,0) was zeroed by the universal reset `.pb-v2.v2-nearby * { padding: 0 }` (also 0,2,0, declared later) on **nearby/tournaments**, so the back arrow + bell sat flush against the frame edges there while every other tab kept the 16px inset (this was the user-reported issue); **(2) back chip** — the `.pb-v2 .v2c-back` border/shadow (0,2,0) was stripped by `.pb-v2.v2-<screen> button { border:none }` (0,2,1) on home/games/clubs/profile but survived on nearby/tournaments, so the Map's back button alone looked outlined/fainter. Fix in `shared/styles/v2.css`: both rules are now scoped under `.v2c-topnav` (`.pb-v2 .v2c-topnav .v2c-inner` / `.v2c-back`, specificity 0,3,0) so they outrank any per-screen `*`/`button` reset; the back chip is borderless + a fixed `box-shadow: 0 3px 12px rgba(26,33,56,0.12)` (not the per-screen `--shadow-sm`), matching the look the majority of tabs already had. CSS-only; no component/markup/JS change, no new screen/permission/API, FILEMAP unchanged. Verified via Playwright at 677px across Map/Home/Games/Tournament/Clubs: `.v2c-inner` padding is **16px** on all (was 0 on Map), and the `.v2c-back` border/shadow/background/radius are byte-identical. |
| 2026-06-24 | **Venue owners can now claim an existing (unclaimed) directory venue instead of creating a duplicate — the claim backend already existed; only the app surface + the owner-side gate were missing.** New `features/owner/ClaimVenueScreen.tsx` (`claim-venue`): a debounced directory search scoped to **unclaimed** listings (`listVenues({ search, state: 'unclaimed' })`) → pick a venue → a proof form (description ≥10 chars + optional links, max 5) → `submitVenueClaim()` (`POST /api/v1/claims`) → a "claim submitted, pending review" success view. Entry points gated by the new permission: a **"Claim"** button beside "Create venue" on `OwnerVenuesScreen.tsx`, and the (previously dead) "Already in our directory? **Claim it instead**" hint on `OwnerNewVenueScreen.tsx` now links here. Screen registered in `navigation.ts` (path `/owner/venues/claim`, `deepLinkParent`→`owner-venues`) + `App.tsx` (render case, `SCREEN_PERMISSIONS`/`SCREEN_AUTH_INTENT`). `submitVenueClaim()` + `VenueClaim` type + a `state?: 'claimed'\|'unclaimed'` filter on `ListVenuesParams` added to `shared/lib/api.ts`. New **`owner.venues.claim`** permission synced to all three copies + `PERMISSION_CATALOGUE` + owner role default, and **backfilled onto the live owner role** via `SYSTEM_PERMISSION_BACKFILLS` (insert-only seeding can't reach existing rows). API change (sibling `api/`): `POST /claims` now gated by `owner.venues.claim` (was auth-only; web only consumes the admin side, so unaffected); `GET /venues` gained a `state=claimed\|unclaimed` filter; both reflected in `/lists`. Verified end-to-end via curl (player **403** / unauth **401** / short-proof **400** / owner **201** / duplicate **409** / admin approve → venue `state:'claimed'` + owner linked, then DB restored). Build + lint clean (new files lint-clean). FILEMAP updated. |
| 2026-06-24 | **Booking flow now surfaces unavailability up front: full-day markers on the calendar + visible "Booked" hours in the time picker.** (1) **Calendar full-day dots** — `shared/components/ui/CalendarDatePicker.tsx` gained `fullDays?: Set<string>` (marked with a coral dot + muted text, still tappable) and an `onMonthChange(year,month)` callback (fires on mount + navigation). `BookCourtScreen.tsx` tracks the visible month (via a stable `useCallback` handler so the effect can't loop) and fetches that month's per-day availability — re-fetched when the chosen court changes (markers are court-scoped when a court is picked, else venue-pool), with a legend under the calendar. (2) **Visible "Booked" hours** — `shared/components/ui/HourSelect.tsx` gained an optional `hourInfo(hour) => { hide?; disabled?; note? }` (back-compat: the old `disabled` predicate still hides, so `CreateGameScreen` is untouched). The **Start** picker now passes `hourInfo` to show booked hours greyed + struck-through with a coral "Booked" tag (past hours still hidden) instead of silently omitting them; the End picker is unchanged. `useVenueAvailability` now also exposes `isPast`/`isFull` so the picker can label *why* an hour is gone. **API (`api/`):** new public `GET /api/v1/venues/:id/availability/range?from&to&courtId` (≤62 days) — one batched bookings query → per-day `{ openHours, freeHours, full, closed }`, bounding "full" to each weekday's opening window from `VenueHour`; mirrors the single-date endpoint's pool/court model. Client: `getVenueAvailabilityRange` + `VenueAvailabilityRange`/`VenueDayAvailability` in `api.ts`. `/lists` updated. **No new permission** (read-only, public like the single-date availability). FILEMAP unchanged (additive — `api.ts`/shared UI keep their responsibilities). Verified end-to-end via curl (range returns closed/free/full + court-scoping) — and a fully-booked demo day was seeded (The Dink Lab, 2026-06-29, all 3 courts 10:00–22:00 via 3 players) so the marker + "Booked" tags are testable; app build + scoped lint clean. |
| 2026-06-24 | **Club polish: cover-photo upload, a member cap, removed a dead "Skill Level" step, and fixed the join-from-list glitch (all app-only — fields already existed server-side).** (1) **Cover photo** — `features/clubs/v2/CreateClubV2.tsx` (upload after create, since the cover needs the new club id: `createClub` → `uploadClubMedia` → `updateClub({ coverImageUrl })`, non-fatal) and `features/clubs/EditClubScreen.tsx` (pick/Change/Remove with a live preview; uploads on save) now set the club's `coverImageUrl`, which the detail hero + list cards already render — previously every club was stuck on the gradient placeholder. (2) **Member limit** — both screens expose an optional `joinLimit` (blank = unlimited); the server already enforces it (a join past the cap 409s). (3) **Removed the dead Skill Level step** — CreateClubV2's step 2 collected skill pills that were never sent and have no model field; replaced with the cover + limit fields (and the step hint reworded). (4) **Join-from-list reconcile** — `features/clubs/v2/ClubsScreenV2.tsx` `doJoin` now reads the join result: a `member` status moves the club into My Clubs and out of Discover immediately (private clubs stay "Requested"), instead of leaving it sitting in Discover until reload. Types: `joinLimit` on `ApiClub`, `coverImageUrl`/`joinLimit` on the create/update payloads (`coverImageUrl: ''` clears it — the server maps falsy→none; it doesn't accept `null` for that field). **No new permission, no API/route/`/lists`/model change** (the Club model already had `coverImageUrl`/`joinLimit`; media upload only needs auth). FILEMAP updated. Verified: build + lint clean; API e2e (8/8 — joinLimit persist/enforce-409/clear, cover persist + clear-via-`''`) and browser e2e (create wizard: no Skill step, cover+limit fields, club persisted joinLimit=5 + a cover; join-from-list moves the club into My Clubs without reload). |
| 2026-06-24 | **Clubs got the three "experience" gaps closed: a live-updating feed, photo/GIF posts, and paginated + server-searched discovery (all backend already supported it — app-only wiring).** (1) **Live feed** — `features/clubs/ClubDetailsScreen.tsx` now opens an `EventSource` to the existing per-club SSE stream (`GET /api/v1/clubs/:id/stream?token=`, built via `apiUrl` + `getAccessToken`) while the screen is open; the server's `post.created/updated/deleted` + `reaction.changed` events drive a 350 ms-debounced `listClubFeed` refetch (+ the open thread's replies), and `member.joined` updates `memberCount` + refetches members — so other people's posts, likes and joins appear without a reload. Server is the source of truth (the actor's own optimistic updates already covered their action); guests (no token) just get a static snapshot. (2) **Photo/GIF posts** — the composer gained an "Add photo" button (hidden file input → `uploadClubMedia(clubId, file)` → staged `draftAttachments` with removable thumbnails, up to 10), and `createClubPost` now sends `attachments`; the feed (posts + replies) renders them via `apiImageUrl`. A post needs text OR an attachment. New `uploadClubMedia` (ownerType `club`) + `ClubAttachment` type + `attachments` on `ApiClubPost` in `shared/lib/api.ts`. (3) **Paginated + searched discovery** — `listClubs` now returns `{ items, cursor }` and accepts `search`/`cursor`/`pageSize`; `features/clubs/v2/ClubsScreenV2.tsx` debounces the search box (300 ms) into a **server-side** `?search=` (so matches span all pages, not just the first 20) and adds a **"Load more"** button that appends the next cursor page (was capped at 1 featured + 6 nearby). `features/clubs/ClubsScreen.tsx` (legacy) adapted to the new `{ items }` shape. **No new permission, no API/route/`/lists`/model change** — the stream, `attachments`, and list pagination/search all already existed server-side; media upload only needs auth (no club-ownership check). FILEMAP updated. Verified: build + lint clean; API e2e (13/13 — search/cursor pagination, photo upload+attach, attachment-only post allowed, empty post 400, SSE delivers `post.created`/`ready`) and browser e2e (6/6 — photo upload→preview→post→renders, another user's post appears live without reload, Load more grows the list 19→23, server search surfaces a club from a later page). |
| 2026-06-24 | **Club hosts can now moderate — approve/deny private-club join requests, remove members, and edit/delete posts & comments (the backend already supported all of it; only the UI was missing).** `features/clubs/ClubDetailsScreen.tsx` gained three host/author surfaces, all on the existing detail screen (no new screen/route/permission): (1) **Join requests** — for a host of a *private* club, the screen now also fetches `listClubRequests` on load (and in `refresh()`); an About-tab banner ("_N people want to join_") and a "Manage requests · N" item in the host ⋯ menu open a `BottomSheet` listing each requester (avatar/name/message/time) with **Approve** (→ `approveClubRequest`, then `refresh()` since the server creates the membership + bumps memberCount + notifies) / **Deny** (→ `denyClubRequest`). This unblocks private clubs, which previously had no way for anyone to be let in. (2) **Remove member** — a host-only "Remove" button on each non-host member row → confirm dialog → `removeClubMember` (drops the row, decrements the count). (3) **Edit/delete posts & comments** — a ⋯ button on every post *and* reply (shown to the author, or the host for delete) opens an action sheet: **Edit** switches the body to an inline textarea (`editClubPost`), **Delete** confirms then soft-deletes (`deleteClubPost`, rendering the existing "deleted" placeholder; a reply also decrements its parent's `replyCount`). Six thin client fns + `ApiClubRequest` added to `shared/lib/api.ts` (`listClubRequests`/`approveClubRequest`/`denyClubRequest`/`removeClubMember`/`editClubPost`/`deleteClubPost`). **No new permission** — host actions are gated client-side by `club.isHost` (author actions by `author.id`) and enforced server-side by host-ownership / `player.clubs.moderate` (already in all three permission copies), mirroring the existing Edit/Delete-club precedent. **No API/route/`/lists`/model change** (every endpoint already existed). FILEMAP updated (clubs slice gained moderation responsibilities). Build clean. |
| 2026-06-24 | **Deleting a game lobby now keeps the court booked and routes to a refund/cancel page (instead of silently cancelling the booking).** Reworks the lobby-delete added earlier today. **App:** `features/games/GameDetailsScreen.tsx`'s "Delete lobby" now calls `deleteGame(id, { keepBooking: true })`, and on success shows a **post-delete success view** ("Lobby deleted — your court is still booked") with a **Refund or cancel booking** button → the new **`booking-refund`** screen (plus "Back to games"); the confirm sheet was reworded ("removes the lobby only — your court stays booked, refund/cancel on the next screen"). New `features/bookings/BookingRefundScreen.tsx` loads the booking (`getBooking`), shows a summary + an honest "refunds aren't automated yet" note, and **Cancel booking & request refund** (`cancelBooking`) → a "Cancellation requested" success state (handles already-cancelled/404). Wired `booking-refund` (path `/bookings/:id/refund`) into `navigation.ts` (union/`pathFromScreen`/`screenFromLocation`/`deepLinkParent`→my-bookings) + `App.tsx` (render case, `SCREEN_PERMISSIONS`/`SCREEN_AUTH_INTENT`), gated by the existing **`player.bookings.create`** (no new permission — ownership is enforced server-side by `cancelBooking`, mirroring how `edit-club` reuses `player.clubs.create`). `deleteGame` client gained a `keepBooking` option + a `{ id, deleted, bookingId }` return. **API (`api/`):** `deleteGame` accepts `?keepBooking=true` — when set it deletes the game but, instead of cancelling the linked booking, converts it from `bookingType:'game'` back to `'court'` (so it returns to My Bookings and is cancellable) and returns the surviving `bookingId`; default behaviour (cancel the booking) is unchanged, so MyGames/inline delete is unaffected. No `/lists` change (same `DELETE /games/:id` route — only an optional query param). Verified end-to-end via curl (book → host game → `?keepBooking=true` → booking survives `confirmed`/`court` + reappears in My Bookings + game 404; default delete still cancels). FILEMAP updated (new BookingRefund screen). |
| 2026-06-24 | **The owner venue editor's active tab now survives a page reload (and is shareable/deep-linkable).** `features/owner/OwnerVenueScreen.tsx` tracked the active tab (Overview/Insights/Bookings/Listing/Location/Hours/Courts/FAQs/Reviews/Photos) in local `useState`, so a reload snapped back to Overview. The tab is now **derived from the URL** (`?tab=<id>`, which `navigation.ts` already parsed/built) instead of state, and switching tabs `goTab()`s via `onNavigate('owner-venue', { id, tab }, { replace: true })` — a **replace** so it doesn't pile up back-button history, and since the screen is keyed by venue `id` it re-renders **without remounting** (no venue refetch). Overview omits the param for a clean URL (`q()` drops `undefined`). Both the tab chips and the Overview drill-down links (`onOpenTab`) route through `goTab`. No API/permission/route/`/lists` change; FILEMAP unchanged (logic-only). Verified via Playwright (click Courts → URL `?tab=courts`; reload → still Courts, Overview's "Revenue this month" absent; direct `?tab=bookings` loads Bookings). |
| 2026-06-24 | **The active v2.1 player screens now show skeleton loaders instead of a "Loading…" string.** The shared `LoadingSkeleton` (Tailwind/v1-token based) was only used by owner/organizer/v1 screens; the v2.1 player surfaces fell back to plain text. New **`shared/components/ui/V2Skeleton.tsx`** renders shimmer placeholders shaped like each screen's *real* cards — it reuses the screen's own wrapper class (`.game-card`, `.club-card`, `.court-card`, `.tt-card`, `.match-item`) and fills the inner content with `.v2sk` blocks, so the placeholder inherits the exact surface/radius/shadow/size. Variants wired in: `home-featured` + `home-discover` ([HomeScreenV2](src/features/home/v2/HomeScreenV2.tsx) — the "Featured Today" hero card *and* the Discover rail; the Featured section was previously gated entirely behind `featured` (=`games[0]`), so during load it was absent and **popped in, shoving Discover down** once games arrived — the skeleton now reserves its space), `game-list` ([GamesScreenV2](src/features/games/v2/GamesScreenV2.tsx)), `club-list` ([ClubsScreenV2](src/features/clubs/v2/ClubsScreenV2.tsx)), `court-list` (featured card + rows, [NearbyScreenV2](src/features/venues/v2/NearbyScreenV2.tsx)), `tournament-list` ([TournamentsScreenV2](src/features/tournaments/v2/TournamentsScreenV2.tsx)), `match-list` ([ProfileScreenV2](src/features/profile/v2/ProfileScreenV2.tsx) Recent Games). The `.v2sk` fill/pulse/reduced-motion + a `--skel` token (light `#E6EAF3`, dark `#2C313B`) live in `shared/styles/v2.css`. No new screen/permission/API. Verified via Playwright at 768px with delayed API responses — all five public screens render correctly shaped skeletons (Profile needs auth, same wiring). FILEMAP updated (new shared UI primitive). |
| 2026-06-24 | **Create-a-venue form slimmed to name + location + contact; courts/pricing moved to the post-create Courts tab; visible Latitude/Longitude fields added.** `features/owner/OwnerNewVenueScreen.tsx` — the **"Courts & basics"** section (Indoor/outdoor, Court count, Surface type, Price from) was removed from the create form; those are configured in the venue editor's **Courts** tab after the venue exists (per the screen's "fill in the rest on the editor after" promise), so creation is just identity + location + contact. The Location section gained editable **Latitude / Longitude** `FormField`s (bound to `lat`/`lng`, auto-set by the map pin / address pick, with a "edit only if you have exact coordinates" hint) so the coordinates the map captures are visible and tweakable — matching the Location editor tab. Dropped the now-unused `FormSelect` import + `IO_OPTIONS` and the `indoorOutdoor`/`courtCount`/`surfaceType`/`priceFrom` form fields. No API/permission/route/`/lists` change (those fields are still accepted on create + editable later; create just sends fewer). FILEMAP unchanged. Verified via Playwright (create flow: name + address pick → City "Tanza", Lat 14.392192, Lng 120.850740 auto-filled, no "Courts & basics" section, sections now Basics → Location → Contact; "Create venue" → lands on the venue editor) + test venue cleaned up. |
| 2026-06-24 | **Venue/court detail (`features/venues/CourtDetailsScreen.tsx`) reworked from a brochure into a booking page.** Added, all from data `getVenue` already returns (no API change): an **"Open today"** availability strip — tappable free-hour chips bounded by today's opening hours via `useVenueAvailability(venue.id, today)` (a chip → `book-court` with `date`+`time` pre-filled; +N-more / "Pick a date" fall through to the full Book flow; shows "Closed today"/"Fully booked"/"See available times" per state); **clearer pricing** — new `priceRangeLabel(venue, courts)` + `currencySymbol` in `shared/lib/venueDisplay.ts` show a per-hour rate or a `₱min–₱max/hr` range when courts differ; an **upfront booking-policy note** above the CTA when `requireBookingApproval` ("owner approves first — pay within {payWindow}", CTA → "Request to book"); a **Photos** scroll strip (`venue.gallery`); **full weekly hours** (collapsible, today highlighted); a **per-court breakdown** (number/type/surface/rate + per-court photo); **review count** (`googleReviewCount`) next to the rating + a **Verified** chip (`isVerified`); **distance from you** in the location card (best-effort `getCurrentLocation` + `haversineKm`/`formatDistance`, silent on denial); a **Contact** section (phone `tel:` + website); and the previously-dead **Share** (Web Share / clipboard) and **Save** (device-local `localStorage` favourite, heart fills) hero buttons now work. **No new permission** — these are read-only enhancements of an already-public browse surface (the booking action stays gated by `player.bookings.create` in the Book flow), mirroring how the same screen's "Games here" needs none. No API/route/`/lists` change; FILEMAP unchanged (no responsibility shift — `venueDisplay` just gained two more formatters). Build clean. |
| 2026-06-24 | **Game lobby gets a host "Delete game" action.** `features/games/GameDetailsScreen.tsx` — the host's disabled "You're hosting" button in the sticky CTA is now a coral **Delete game** button (gated by `player.games.manage`; hosts without it keep the "You're hosting" badge) that opens a confirmation `BottomSheet` ("this can't be undone — the court is released and the roster is notified") and calls the existing `deleteGame` → `DELETE /games/:id`, then lands on the Games tab (`replace`). No API/permission/route/`/lists` change (endpoint + perm already existed); FILEMAP unchanged. Build clean. |
| 2026-06-24 | **Edit Profile hides the "Skill level" picker for non-players (owners, organizers, admins).** `features/profile/EditProfileScreen.tsx` showed the DUPR/skill-tier picker to everyone, but a venue owner or event organizer isn't a player — it didn't belong there. A `showSkillLevel` gate (`!owner.access && !organizer.access && !admin.access`) now renders the `FormTierPicker` only for players/coaches, and the submit omits `skillLevel`/`skillLevelLabel` for the gated roles so an owner's profile save never writes a skill level. No new permission/API/route; FILEMAP unchanged (logic-only). Verified via Playwright across all five quick-login roles: skill picker absent for owner + organizer (+ admin), present for player + coach. |
| 2026-06-24 | **The app now fills the screen on tablets instead of sitting in black gutters.** Follow-up to the 2026-06-23 phone-frame cap: that capped `.app` at a fixed 576px on every viewport, so on a tablet the surround showed big solid-black (`#000`) bars (the user reported it as broken). `shared/styles/index.css` now keeps `--frame-max: 576px` on phones but widens it to **1000px on tablets** via a viewport `@media (min-width: 577px)` (a `@media`, not a `@container` — the frame's own width depends on `--frame-max`, so the cap must key off the device, not the frame). The app fills the viewport edge-to-edge up to 1000px, so portrait tablets (768/810/834) and most landscape tablets show **no gutters**; the 1000px cap sits just under the **1024px** `@container app` desktop breakpoint, so the legacy sidebar layout (index.css desktop blocks + the v2 tab-bar hide in `v2.css`) stays dormant — the app keeps its **mobile single-column layout, only wider** (the 640/768px container blocks just enlarge a few elements once the frame is wide enough). Beyond 1000px (large landscape tablets / desktop) the frame stays a centred 1000px column with the existing black surround (was 576px, so desktop now shows *less* black, not more). No new screen/permission/API; FILEMAP unchanged (no responsibility shift). Verified via Playwright at 390/768/1024/1440: app width 390/768/1000/1000px, sidebar never activates, mobile tab bar/FAB pin to the frame at every width. |
| 2026-06-24 | **Venue address field is now a true type-ahead autocomplete (live suggestions, not search-on-submit).** New reusable `features/owner/components/AddressAutocomplete.tsx` — a debounced (350ms) combobox that, as the owner types (≥3 chars), fetches ranked place suggestions and shows them inline; picking one fills the address, **drops the map pin, flies the map there, and auto-fills city + area/district** from the suggestion's parsed parts (no reverse-geocode round-trip). Keyboard (↑/↓/Enter/Esc) + outside-click close; aborts superseded lookups via `AbortSignal`. Wired into **`OwnerNewVenueScreen.tsx`** (replaces the plain "Full address" text field; `MapPinPicker` now gets a `flyTo` target so the map recenters on pick) and the **`tabs/LocationEditorTab.tsx`** pin search (replaces the old type-then-press-"Search" button + `geocodePlace` single-hit flow with the live list). API client: `suggestPlaces()` + `GeocodeSuggestion` added to `shared/lib/api.ts`; the request layer (`RequestOptions`/`rawRequest`) gained `signal` passthrough so type-ahead lookups can cancel. API change (sibling `api/`): new **`GET /api/v1/geocode/suggest`** (`geo` feature) — a ranked Nominatim list (`limit` 1–10, `addressdetails`) returning lat/lng + label + city/region/area per hit, 1h cache like the existing geocode proxy; added to `/lists`. **No new permission** — it enhances the existing `owner.venues.create`/`owner.venues.manage` flows, and the geocode proxy is an unauthenticated read like `/geocode` + `/geocode/reverse`. Verified end-to-end via curl + Playwright (type "SM Mall of Asia" → suggestions → pick → pin at 14.5352,120.9816 + "Detected city: Pasay · Metro Manila" + area "Zone 10"). **Follow-up (same day): a detected city that isn't one of our seeded cities is now *captured*, not nagged.** Previously an unlisted city (e.g. Tanza · Cavite) showed "not in our list, pick below" and left the City dropdown on a stale/wrong value — defeating the point of address capture. Now `OwnerNewVenueScreen`'s `applyDetectedCity(city, region)` helper (shared by the pin reverse-geocode + the address pick) links the dropdown when the city matches a seeded one, otherwise **clears `cityId` and saves the detected "City, Province" as free-text `region`** (which the public page already shows via `venueDisplay.locationLine`'s `city || region` fallback). Copy reworded to "Detected city: … — saved from your address. Optionally link a listed city below"; the City field is relabelled optional with a "Use my address (no listed city)" blank option, and manually linking a listed city clears the captured free-text. No model/API change (the Venue `region` field + `createVenueSchema` already accept it). Verified via curl (create w/ region + no cityId → `region: "Tanza, Cavite"` persists & returns) + Playwright (unlisted city no longer nags; dropdown no longer shows a stale city). **Follow-up #2 (same day): the confusing "Area / district" field was replaced with a standard address form.** The lone "Area / district" input (placeholder "e.g. Makati" — looked like a duplicate City, and only the type-ahead path ever filled it) is gone; the Location section now has **Address line 1**, **Address line 2**, and a **Postcode** field (the latter paired beside City). Both geocode endpoints now also return `line1` (house_number + road) and `postcode`, so the helper — now `applyDetectedPlace(parts, replace)`, shared by the address pick (replace=true, authoritative) and the map-pin reverse-geocode (replace=false, fill-blanks-only so a pin nudge doesn't wipe typing) — auto-fills Address line 1 + Postcode (Address line 2 stays manual). The type-ahead label changed "Full address" → "Search address". API change (sibling `api/`): new `addressLine1`/`addressLine2` String fields on the Venue model + `create`/`updateVenueSchema` (`postalCode` already existed; all spread through the existing projections — no new route, `/lists` geocode descriptions refreshed); `geo` controller's `suggest` + `reverse` now emit `line1`/`postcode`. Verified via curl (create w/ addressLine1/2 + postalCode → persist & return) + Playwright (pick "SM City Tanza" → Address line 1 "Antero Soriano Highway", Postcode "4108" auto-filled). **Follow-up #3 (same day): City is now a free-text field, not a dropdown.** The seeded-city `FormSelect` couldn't show an unlisted detected city — it sat on "Use my address (no listed city)" even after the map detected one, which read as broken. City is now a plain `FormField` bound to a new free-text `cityName` that the map/address fills directly (shows "Tanza"); `applyDetectedPlace` writes `cityName` straight from the detected place and **silently links a seeded `cityId` only when the typed name matches one** (so city filtering still works), and typing in the field re-runs that match. The unlisted-city value moved from `region` to its own `cityName` (region now holds just the province). API change (sibling `api/`): new free-text `cityName` on the Venue model + `create`/`updateVenueSchema`; the list + detail projections surface `city` as `cityId?.name ?? cityName` so the public page shows it. Verified via curl (create w/ `cityName:"Tanza"` + no cityId → detail returns `city:"Tanza"`) + Playwright (pick "SM City Tanza" → City field shows "Tanza", not "no listed city"). FILEMAP (app + api) updated; task tracker `TASKS/owner-venue-tasks.md` demo-checklist item ticked. |
| 2026-06-24 | **Venue booking link is now system-generated, not hand-typed (+ optional custom slug).** Owners used to paste a free-text `bookingUrl` ("Where players reserve a court"); the system now generates a canonical booking link from the venue slug (`…/venues/<slug>`, built client-side off `window.location.origin` like the club share link) that deep-links the PWA to the venue's court page where players book. `features/owner/tabs/ListingEditorTab.tsx` Contact & booking section: the free-text field is replaced by a read-only link with **Copy** + **Share** (native `navigator.share` → clipboard fallback) and an optional **custom slug** input (`/venues/<…>`) that saves as `bookingSlug` (live-normalized to kebab-case to mirror the server; a `SLUG_TAKEN`/`INVALID_SLUG` `ApiError` surfaces an inline message). The custom-slug field is **checked live as you type** — a 400ms-debounced call (fired from the input's onChange, request-id-guarded against stale responses) to a new `GET /venues/:id/booking-slug-available?slug=…` (owner-gated) shows a spinner → "Available" (green) / "Already taken" / "Use letters, numbers and hyphens", colours the field border, and **disables Save** while a taken/invalid slug is entered (so one bad slug can't 409 the whole listing save). The link + Copy/Share controls are a new reusable `features/owner/components/BookingLinkShare.tsx`, surfaced **both** as the very first card on the venue **Overview** tab (`tabs/VenueOverviewTab.tsx` — "share it instantly", with a "Customize" link into the Listing tab) **and** inside the Listing editor's booking block (above the custom-slug input). The free-text "Booking link" field was removed from `OwnerNewVenueScreen.tsx` (the link is auto-made post-create, with a hint) and `components/CompletenessMeter.tsx`'s "Website or booking link" check became "Website" (the booking link always exists now). **No new permission** — editing the slug is part of the existing `owner.venues.manage` listing edit, mirroring how the booking-approval toggle reused it. API change (sibling `api/`): new `bookingSlug` field on the Venue model + `updateVenueSchema`; `updateVenue` normalizes + uniqueness-checks it across every venue's `slug` AND `bookingSlug` (409 `SLUG_TAKEN` / 400 `INVALID_SLUG`, empty clears it); `getVenue` + `resolveVenueId` now also resolve a venue by `bookingSlug` so the custom link lands; new `GET /venues/:id/booking-slug-available` for the live typing check (normalize + uniqueness shared with `updateVenue` via `normalizeBookingSlug`/`isBookingSlugFree`) + `/lists` entry. Verified end-to-end via curl (set/normalize/collide-vs-slug-and-bookingSlug/invalid/clear + link resolves on a public venue + availability check: available/taken/invalid/empty/self-excluded/401). App FILEMAP updated (new `BookingLinkShare` component); api FILEMAP updated (booking-slug endpoints). |
| 2026-06-23 | **Club feed posts are now commentable (the comment icon was a dead element).** `features/clubs/ClubDetailsScreen.tsx` — the feed's message/count `<span>` was static, so there was no way to comment even though the API has always backed it (a reply is just a post with a `parentPostId`). It's now a toggle button that expands an inline comment thread per post: lazy-loads replies on first open (new `listClubReplies` → `GET /api/v1/clubs/:id/posts/:postId/replies`, page size 50, reversed to read oldest-first), shows them with avatar/name/time, and — for members who can post — a "Write a comment…" composer that submits via the extended `createClubPost(id, body, parentPostId)` (now passes `parentPostId` through to the existing `POST …/posts`). New comments append optimistically and bump the parent's `replyCount`; non-members see the thread read-only with a "Join this club to comment." hint. **No new permission** — a comment is a reply, server-gated by the existing `player.clubs.post` (same capability as posting), so commenting reuses it. No API route/`/lists` change (the reply routes already existed) and no FILEMAP change (logic-only within files that already own the club feed + club API client). Build clean. |
| 2026-06-23 | **Club hosts can now edit their club from the ⋯ menu (next to Delete club).** New `features/clubs/EditClubScreen.tsx` — a single-page editor that loads the club (`getClub`), guards to the host (`club.isHost`; non-hosts/guests get a friendly "can't edit" state), prefills name/description/visibility, and saves via the new `updateClub` client (`PATCH /api/v1/clubs/:id`, already host-or-moderate-gated server-side). The host branch of the club-detail ⋯ menu (`ClubDetailsScreen.tsx`) now shows **Edit club** above **Delete club**. On save it just `onBack()`s — the URL-routed nav unmounts the editor and remounts the detail, which refetches the updated club. New `edit-club` screen wired in `navigation.ts` (path `/clubs/:id/edit`, `clubs` parent) + `App.tsx` (`renderScreen` case, `tabForScreen` → clubs). **No new permission and no API/`/lists` change** — the edit screen reuses `player.clubs.create` as its static gate (mirroring how `invite-players` reuses `player.games.create`) and the real authorization is host-ownership, matching the sibling Delete action (which also has no dedicated permission). FILEMAP updated. |
| 2026-06-23 | **Player Tournament tab's three tabs are now role-aware (the middle "Upcoming" tab is gone).** `features/tournaments/v2/TournamentsScreenV2.tsx` — the tabs now mean different things per role instead of showing the same public Open/Upcoming/Results to everyone. **Open** = events open for registration that you're *not already part of* (organizers: ones they don't run; players/coaches: ones they haven't joined). **Middle tab** is relabelled + repurposed: organizers get **"Managing"** (their tournaments that haven't finished — an active management shortlist sourced from `listMyTournaments`), players/coaches get **"Joined"** (the active tournaments they've registered for). **Results** is unchanged (personal: finished events you hosted + finished public ones you joined). An organizer's own cards now open the organizer console (`organizer-tournament`) instead of the player detail; the "Managing" thumbnail badge is suppressed inside the Managing tab (redundant there). To make Open's exclusion + the Joined tab correct *without* probing `my-registration` tournament-by-tournament, the screen now fetches the user's registrations in one call (new `listMyTournamentRegistrations` → `GET /api/v1/tournaments/registrations/mine`), eagerly on load, replacing the old lazy completed-only probe. No new permission (reorganises the existing browse/join + `organizer.tournaments.manage` surfaces — no new capability). API change (sibling `api/`): new read-only `getMyTournamentRegistrations` controller + `/tournaments/registrations/mine` route (static segment registered before the `/:id` matcher) + `/lists` entry; verified end-to-end via curl (player + organizer registrations, 401 without auth). FILEMAP updated. |
| 2026-06-23 | **Tournament browse cards now show a "Managing" badge on tournaments the signed-in organizer runs.** `features/tournaments/v2/TournamentsScreenV2.tsx` already loaded the user's own tournaments (`listMyTournaments`, organizers only) for the Results tab; that set is now reused to mark *every* card the user manages across the Open/Upcoming/Results lists — a top-left white pill (crown icon + "Managing") on the card thumb (`mineIds.has(t.id)`), styled by a new `.tt-mine` rule in `shared/styles/v2.css` (positioned clear of the bottom-center status chip; readable over both the gradient and a banner image). Non-organizers never fetch `mine`, so they never see it. No new permission (visible only to holders of `organizer.tournaments.manage`, which already gates managing tournaments), no API/`/lists` change, no FILEMAP change (no responsibility shift). |
| 2026-06-23 | **Tournaments now have a participant group chat (roster: organizer + every registrant), mirroring the game-roster chat.** New `features/tournaments/v2/TournamentChatScreen.tsx` (same realtime + send/append model as `GameChatScreen`) reachable from the player tournament detail (a blue "Tournament chat" button shown to registered players) and the organizer tournament detail (a "Chat with participants" button once there are registrants). Realtime: `useRealtimeStream` listens for the SSE event `tournament.message.created` → `emitRealtime('tournament.message')`; the screen subscribes via `onRealtime`. New `tournament-chat` screen wired in `navigation.ts` (+ path `/tournaments/:id/chat`) and `App.tsx` (`renderScreen` case, `tabForScreen` → tournaments, `SCREEN_PERMISSIONS`/`SCREEN_AUTH_INTENT`). API client added to `shared/lib/api.ts`: `ApiTournamentMessage` + `listTournamentMessages`/`sendTournamentMessage` (`GET|POST /api/v1/tournaments/:id/messages`). New **`player.tournaments.chat`** permission synced to all three copies + `PERMISSION_CATALOGUE` + `PLAYER_BASE_PERMISSIONS` (so player/coach/owner/organizer get it); also granted on the live DB roles that already hold `player.games.chat`. API change (sibling `api/`): `TournamentMessage` model + `listTournamentMessages`/`sendTournamentMessage` in `content` (read = roster, post = roster + `player.tournaments.chat`; fans out a realtime event + a collapsed `tournament_message` notification to the rest of the roster) + routes + `/lists`. Verified end-to-end via curl (organizer posts → participant reads+replies → organizer reads; non-participant 403). |
| 2026-06-23 | **Tournaments can carry an optional banner image (trophy placeholder stays the fallback).** Organizers can now attach a banner when creating a tournament and add/swap it afterwards: `features/organizer/tournaments/CreateTournamentScreen.tsx` gained an "Image (optional)" section (local file pick + preview; uploaded only after create since the media record needs the new tournament id — `createTournament` → `uploadTournamentMedia(id, file)` → `updateTournament(id, { bannerUrl })`, and a failed upload is non-fatal so the tournament still saves); `features/organizer/tournaments/TournamentDetailScreen.tsx` gained a banner hero with an "Add image"/"Change" button (same upload→persist→refresh path). New `uploadTournamentMedia()` in `shared/lib/api.ts` (thin wrapper over the existing `media/upload` with `ownerType: 'tournament'`, already allowed server-side). The player browse/detail screens already rendered `bannerUrl` with a trophy fallback, so this just lets organizers actually set one. No API route change (media-upload + tournament create/update already accept `bannerUrl`; no `/lists` edit); no new permission (covered by the existing `organizer.tournaments.manage` that already gates these screens); FILEMAP unchanged (no responsibility shift). |
| 2026-06-23 | **App capped to a centred phone-width frame on desktop (no more stretching).** The PWA is mobile-only, so on wide viewports it now renders as a centred `--frame-max` (576px) column with the surround filled black, instead of expanding into the old ≥1024px sidebar layout. Implemented in `shared/styles/index.css`: `.app` gains `max-width: var(--frame-max)` + `margin-inline:auto` and becomes a query container (`container: app / inline-size`); the tablet/desktop responsive blocks (`@media (min-width: 640/768/1024px)`) were converted to **`@container app (...)`** so they key off the *frame* width (always ≤576) and stay dormant — the app always renders its mobile layout (sidebar hidden, floating tab bar shown). `#root` background is black for the gutters. `.app` also gets `transform: translate(0)` so it's the containing block for `position: fixed` chrome — otherwise the bottom nav / FAB / sticky CTAs / sheets / offline banner stay anchored to the viewport and bleed across the gutters (container-type alone didn't establish the fixed-CB in all engines). `v2.css`'s `@media (min-width:1024px)` (which hid the v2 bottom tab bar on desktop) likewise became `@container app`, so the tab bar now stays inside the frame. To re-enable the desktop layout, raise `--frame-max` past 1024px. No new screen/permission/API; FILEMAP unchanged (no responsibility shift). Verified at 1440×900 via Playwright (`.app` 576px @ x=432, `#root` black, sidebar `display:none`, v2 tab bar 576px @ x=432). |
| 2026-06-23 | **Player Tournament tab is now live (browse + join/leave), replacing the coming-soon placeholder.** New `features/tournaments/v2/TournamentsScreenV2.tsx` (browse public tournaments grouped Open/Upcoming/Results, in the v2.1 chrome) + `v2/TournamentDetailScreen.tsx` (banner, status, schedule/venue/format/entry/prizes/rules, capacity bar, **Register/Withdraw**, and the organizer's announcements once registered) + `tournamentDisplay.ts` (local status/format/date/money formatters — keeps the slice from importing the organizer feature). Tournament client added to `shared/lib/api.ts`: `listPublicTournaments`/`getMyTournamentRegistration`/`registerForTournament`/`withdrawFromTournament` (reusing the existing `/api/v1/tournaments` routes — register is a **free join**, the organizer marks `paid` manually). `TournamentStatus` gained legacy seed statuses `open`/`closed` (the API treats `open` as a joinable public status — registration is robust to both). New screen `tournament` wired in `navigation.ts` + `App.tsx`; `tourney-soon` CSS replaced with `tt-*` card/badge/empty styles in `v2.css` (`tt-badge`/`tt-reg-banner` are bare-class since the detail renders in the standard `pb` frame). **Role-gated:** `canSeeTournaments = !isOwner && !isAdmin` — players/coaches/organizers get the tab (join/leave only); **owners/admins don't** (the Tournament tab is dropped from `V2TabBar` via new `chrome.tabIds`, from the owner `TabBar` and desktop `Sidebar` via a new `showTournaments` prop, and the `tournaments`/`tournament` screen cases fall back to home for them). Tournament **CRUD stays organizer-only** in `features/organizer/tournaments/` (gated by `organizer.tournaments.manage`). Organizers also get a **"Create a tournament"** shortcut at the top of the browse tab (→ `organizer-tournament-new`) + a "Manage my tournaments" link (→ `organizer-tournaments`), so they don't have to detour through Profile → Organizer console; players/coaches never see it (`userHasPermission(user, 'organizer.tournaments.manage')`). No new permission — browse is public (like games), join reuses the existing `player.tournaments.join`. API change (sibling `api/`): `registerForTournament` now accepts the legacy `open` status too (it's already in `PUBLIC_TOURNAMENT_STATUSES`), so discoverable seed tournaments are genuinely registerable; no route-surface/`/lists` change. Verified end-to-end via curl (register → my-registration → withdraw) and Playwright screenshots (48 cards render; tab present for player, absent for owner + admin). |
| 2026-06-19 | **Per-venue booking approval (request-to-book).** Owners can now require approval before a court booking is scheduled, configured per venue. **Owner:** a "Booking policy" section in `features/owner/tabs/ListingEditorTab.tsx` — a "Require my approval for bookings" toggle + a pay-window select (1/12/24/48/72 h), saved via `updateVenue` (`requireBookingApproval` + `bookingPayWindowHours`). The bookings inbox (`OwnerBookingDetailSheet`/`components/OwnerBookingRow`) now **Approves** a request (→ `awaiting_payment`) instead of confirming it. **Booker:** `BookCourtScreen` detects `selected.requireBookingApproval` and switches the last step to **"Request booking"** — it keeps the card form (the masked brand+last4 is saved on the request via `createBooking({ card })`) but **does not charge**, landing on the existing "requested — awaiting approval" screen. After the owner approves, the player gets a notification and pays from **`MyBookingsScreen`** (new "Pay {amount}" footer on an `awaiting_payment` booking, showing the "Pay by …" deadline; reuses `checkout`). New booking status **`awaiting_payment`** threaded through `bookingDisplay` chips (`statusChip`/`bookingStatusChip`/`bookingPhaseChip` → "Awaiting payment" / "Pay to confirm") and the `BookingStatus` type. The **"Game On → Host a lobby"** picker now requires a `confirmed` booking (pending/awaiting-payment courts can't host yet). **No new permission** — the toggle is gated by the existing `owner.venues.manage`, accept/decline by `owner.bookings.manage`, pay by `player.bookings.create`. API change in sibling `api/` (Venue `requireBookingApproval`/`bookingPayWindowHours`; Booking `awaiting_payment`/`paymentDueAt`/`savedCard`; `createBooking` holds approval venues as requests; owner approve sets the pay-window + notifies; `checkout` guards pay-before-approval / lazy-expires overdue holds). No `/lists` change (reuses existing routes). Verified end-to-end via curl (request → reject-early-pay → approve+notify → pay → confirmed; + lazy expiry). *(Web's owner/booker UI is a separate follow-up — the API change already applies to it.)* |
| 2026-06-19 | **v2.1 "Game On" create flow — join a game or host a lobby on a booked court.** The home Create-Game button (and FAB) is relabelled **Game On** and now opens a chooser instead of jumping straight to the form. New `features/games/v2/CreateChoiceSheet.tsx` (an app-level `BottomSheet`, mounted in `App.tsx`; `handleCreate` opens it for `playerV2`, classic/New keep the direct form): **Join a game** → Games browse/discover; **Host a lobby** → loads the user's **hostable bookings** (`listBookings` filtered to non-cancelled, upcoming, not-already-hosted via `listGames({mine:true})` `bookingId`s) and lets them pick one — or, with none, routes to **Nearby** to book first. `CreateGameV2` was **reworked from venue-first pay-to-book to lobby-on-booking**: it takes a `bookingId`, loads the reservation (`getBooking`), locks venue/date/time as a read-only "Your booked court" card, drops the court picker + date/time pickers + payment (no more `createBooking`/`checkout`), and submit is just `createGame({ bookingId, … })` (booking + payment now happen up-front in the Book flow). **Seamless hand-off** for the no-booking path: a new `intent: 'lobby'` param threads `nearby` (shows a "Select a court to book — you'll set up your lobby right after" banner) → `court-details` ("Book & set up lobby" CTA) → `book-court` (its confirmation leads with **"Create your lobby"**, replacing into `create-game` with the fresh `bookingId`). The `Navigate` type was generalised to support **optional** screen params (so `nearby` is callable both bare as a tab and with `{intent}`) via a single conditional-rest-tuple signature (replacing the two-overload `ScreensWith/WithoutParams`). No new permission — Join reuses browse/join, Host reuses `player.games.create` + `player.bookings.create` (no new capability). No API/`/lists` change (uses existing `getBooking`/`createGame`/`listBookings`). |
| 2026-06-19 | **v2.1 Nearby keeps the user's location in view, and tapping the map focuses on it.** `features/venues/v2/NearbyScreenV2.tsx` — the list sheet overlaps the map's lower edge, so `FrameMap` now measures how many pixels of the map the sheet covers (`map.getContainer()` bottom vs the sheet's `getBoundingClientRect().top`) and feeds that as `paddingBottomRight` to `fitBounds`. Result: when the list is **open**, the user's "you are here" dot is framed in the *visible* strip above the list (previously it sat centred behind the sheet, hidden); when the list **collapses**, the user re-centres into the now-large visible map. The sheet's height transition (300ms) drives a re-frame via a `transitionend` listener so the map pans in sync as the list opens/closes. New `CollapseOnMapClick` (`useMapEvents({ click })`) dismisses the list when the user taps the visible map — which, through the same re-frame, recentres on their location; it's a no-op when the list is already collapsed so the map pans freely. The nearest-court popup now opens only when the list is collapsed (map focused), keeping the thin open-list strip clean. No new permission (refines the existing `player.venues.locate` flow), no API/`/lists` change, no FILEMAP change. |
| 2026-06-19 | **Animated launch splash on cold start.** New `features/auth/SplashScreen.tsx` (+ `splash.css`) — a self-contained React port of the standalone `pickleballers-splash.html` mockup: a pickleball is served into the PickleBallers wordmark, court lines + floating badges reveal behind it, then a circular wipe hands off to the app. Fully scoped under `.pb-splash` (generic class names + `splash-*` keyframes) so nothing leaks into app styles; the iOS phone-frame chrome from the mockup is dropped (the splash fills the app viewport). The animation logic is ported into one `useEffect` driven by a root `ref` + scoped `querySelector` (no global `id`s, StrictMode-safe). Mounted in `App.tsx` as a **once-per-session** overlay (sessionStorage `pb-splash-seen`) on top of everything — the app mounts behind it, so session restore runs during the intro. The app waits for the **"Let's Play"** CTA tap; the same component takes `auto`/`wide` props used by the web build (web auto-dismisses, full-width). Respects `prefers-reduced-motion` (skips to the brand). No new permission — it's a universal cold-start surface shown to guests too (like `LandingScreen`), so it isn't permission-gated. No API/`/lists` change. (Sibling `web/` got the matching splash in the same task per the explicit "apply on both" request.) |
| 2026-06-18 | **v2.1 Settings gains persisted search radius + profile privacy.** Two more account settings now save server-side: a default "Near me" **search radius** (preset 5/10/25/50, stored as `preferences.searchRadiusKm` in km, labelled in the user's unit) and **profile privacy** (`privacySetting`: public/friends/private). Both render as segmented controls in `features/profile/v2/SettingsScreenV2.tsx` and persist optimistically via `authStore.updateProfile()` (radius → `PATCH /me { preferences: { searchRadiusKm } }` dot-path-merged; privacy → top-level `PATCH /me { privacySetting }`), rolling back + showing the inline error on failure. `NearbyScreen` seeds its distance-cap filter from `currentUser.preferences.searchRadiusKm` (falls back to `DEFAULT_NEARBY_KM` for guests). Types threaded: `UserPreferences.searchRadiusKm` + `PrivacySetting` + `AppUser.privacySetting` in `shared/lib/permissions.ts`; `ApiUser.privacySetting`, `toAppUser` (`normalizePrivacy`), and `ProfileUpdate` in `shared/lib/api.ts`. API change (sibling `api/`): `searchRadiusKm` added to the `preferences` sub-doc (`auth.model.ts`), the `updateProfileSchema`, the `/me` payload (default 10), and the dot-path merge in `updateMe`; `privacySetting` was already supported. Verified end-to-end via curl (set/partial-merge/reset). No `/lists` change (route surface unchanged); no new permission (Settings is gated by `player.profile.manage`); no FILEMAP change (no responsibility shift). |
| 2026-06-18 | **Dark mode now applies to the v2.1 design (Appearance toggle works on all screens).** The `useTheme` toggle (Light/Dark/System → `data-theme` on `<html>`, per-device localStorage) only re-themed v1 screens; the v2.1 system in `shared/styles/v2.css` was authored light-only (each `.pb-v2.v2-<screen>` scope redefines the same neutral tokens, none keyed off `data-theme`), so most of the app ignored the setting. Added a dark palette (dark-navy, matching v1's `index.css` dark) appended to `v2.css` for both `[data-theme="dark"] .pb-v2` and `@media (prefers-color-scheme: dark) :root:not([data-theme="light"]) .pb-v2` (specificity ties/out-ranks the per-screen scopes; appended-last wins the tie). The overloaded `--ink` (used as **both** primary text **and** text-on-lime) is disambiguated: `--ink`/`--navy`/`--text-*` flip light; two stable tokens added — `--on-accent` (always dark, forced onto lime fills like `.v2c-join`/`.fab`/`.join-btn`/`.submit-btn`/etc. in the dark block) and `--ink-fill` (dark fills carrying light text: `.chip.active`, the `.pro-banner` gradient). Inline lime+`--ink` styles in `ProfileScreenV2`/`SettingsScreenV2`/`HomeScreenV2` switched to `--on-accent` (inline beats the CSS override). Theme stays **per-device & private** (localStorage; only the picker's own view changes). No new permission (Appearance is an existing control under `player.profile.manage`); no FILEMAP change (no responsibility shift); no API/`/lists` change. |
| 2026-06-18 | **v2.1 Settings now persists account preferences (shell → full).** `features/profile/v2/SettingsScreenV2.tsx` gained real, saved settings: a "Notify me about" section (game reminders / chat messages / announcements toggles) and a distance-units segmented control (km / mi). Changes apply optimistically and persist via `authStore.updateProfile()` → `PATCH /me { preferences }`, rolling the local mirror back + showing an inline error if the save fails. Theme stays client-side (`useTheme`/localStorage) as before. New shared `UserPreferences` type + `DEFAULT_PREFERENCES` live in `shared/lib/permissions.ts` (next to `AppUser`, which now carries `preferences`) so `api.ts` can reuse them without a circular import; `ApiUser`/`ProfileUpdate`/`toAppUser` thread preferences through. API change (sibling `api/`): a `preferences` sub-document on the user model (`auth.model.ts`), surfaced on `/me` with defaults, accepted by `PATCH /me` and **dot-path-merged** so a partial update never wipes sibling prefs. No `/lists` edit (route surface unchanged); no new permission (settings is already gated by `player.profile.manage`). |
| 2026-06-18 | **Player payment history + spend report.** New `features/profile/PaymentHistoryScreen.tsx` — reads the current user's real payments (`listPayments` → `GET /api/v1/payments`, self-scoped, no API change) and renders a report: total-spent / this-month KPIs, a **6-month spend bar chart** (`shared/components/ui/Chart.tsx` `BarChart`, paid vs pending segments), and a receipts list tagged Paid/Pending/Refunded. Both `'completed'` and seeded `'paid'` count as paid spend. Entry rows added to `ProfileScreen.tsx` (Activity menu) and `v2/ProfileScreenV2.tsx` (Account settings list), both gated by the new **`player.payments.view`** permission (synced to api/web copies + `PERMISSION_CATALOGUE` + player role default; resolved client-side so existing players get it on next login). Screen registered in `navigation.ts` + `App.tsx` (`SCREEN_PERMISSIONS`/`SCREEN_AUTH_INTENT`). Money/status formatters inlined locally (profile slice must not import the bookings slice). Brings the app to parity with web's `/dashboard/payments`. |
| 2026-06-08 | **Court detail's last two demo pieces wired live.** `CourtDetailsScreen`'s **location card** now renders a real (non-interactive) Leaflet map centred on the venue's coordinates (`venueCoords`) with a marker — falling back to the old decorative pin box only when a venue has no lat/lng. Its **"Games this week"** block (a hardcoded 3-game array all linking to game id `1`) is replaced by a live **"Games here"** list from `listGames({ venueId })`, with loading/empty/error states; small game-row formatters are inlined locally (the venues slice must not import the games slice's `gameDisplay.ts`). API change (sibling `api/`): the games **list** filter, when `venueId` is passed, now matches a game's fixed `venueId` **or** its lobby-voted `winningVenueId` and drops the published-only default (just hides cancelled) — so games that voted+booked a venue surface on that venue (the app's create flow only ever stores a vote-flow game, so `winningVenueId` is how a game ends up "at" a venue). No route surface change (no `/lists` edit); no new permission (read-only surface of already-public game browse + venue location). |
| 2026-06-08 | **Nearby "Near me" switched to kilometres + map centres on the user.** The distance filter is now km (`maxDistanceKm`, slider 1–50 km, default 10 km; dropped the miles↔km conversion since venue distances are already km). On the map, once located, `FrameMap` keeps the **user** centred (a box symmetric about them, grown to the nearby cluster) instead of fitting the courts' centroid, and the map now renders **every** locatable court when located (the radius narrows only the list) so courts beyond the radius stay visible. No API change. |
| 2026-06-08 | **Home "Open games near you" + "Courts to book" wired live.** Both lists on `HomeScreenRefined` now render real data instead of hardcoded arrays. "Open games near you" reuses the hero's `listGames({ status: 'published' })` feed (drops the game already featured in the hero, joinable only, top 4); cards derive tag/title/time/venue/spots from `ApiGame` and show the day instead of a faked distance. "Courts to book" (renamed from "Courts available now") fetches `listVenues({ pageSize: 6 })` and shows real name, media image (`apiImageUrl`, gradient fallback), area, and a real stat (rating → price → court count) — the fake "X Open" availability badge is gone (no availability endpoint). Both own loading skeletons + empty states; the shared error retry now refetches. Check-in banner + streak card remain demo. No API change. |
| 2026-06-08 | **Home hero wired to live games + bookings.** `HomeScreenRefined`'s state-aware hero now reads real data instead of demo constants. `CommitmentHero` shows the player's soonest upcoming **commitment** — the earliest across their games (`listGames({ mine: true })`) and **court bookings** (`listBookings()`), unified via `NextCommitment` (`kind: 'game' \| 'booking'`); a booking renders the same hero (party size instead of a roster, primary action opens My bookings, Directions → the venue). When there's no commitment it falls back to the best open game from `listGames({ status: 'published' })` (`FindGameHero`), else the create-a-game prompt. Guests skip the `mine`/bookings fetches. Small game/booking formatters are inlined locally (home must not cross-import the `games` slice's `gameDisplay.ts` per the shared-only rule). The rest of the home screen (Open games near you, Courts available now, check-in banner, streak card) stays demo. No API change. |
| 2026-06-05 | **Owner Nearby tab → "your venues" operations map.** Owners get `features/owner/OwnerNearbyScreen.tsx` on the Nearby tab instead of the player discover view — their venues plotted as live-status pins (amber = pending approvals, green = active today, slate = quiet; today's booking count inside each pin). Tapping a pin shows a glance (today's bookings / pending / occupancy) with Manage + Bookings actions; an attention-sorted venue list (pending first) sits below. Reuses `useOwnerDashboard` (venues + per-venue glance/analytics) — no new fetch, no competitor data. New `owner.market.view` permission gates it (`App.tsx` branches the `nearby` case); synced to api/web. (Superseded the earlier same-day market/competitor map concept — `marketMetrics.ts` removed.) |
| 2026-06-04 | Added a **court-booking flow** (new `features/bookings/` slice: `BookCourtScreen`, `MyBookingsScreen`, `bookingDisplay.ts`). Pick a **priced** court → date/time/duration → live cost (`priceFrom × hours`) → review → **checkout**. Checkout reads a new server setting (`GET /api/v1/settings`): in **test mode** it pre-fills the 4242 demo card, shows a TEST banner, and `POST /api/v1/payments/checkout` completes the payment + confirms the booking (no charge); live mode leaves it pending. Entry points: a "Book this court" CTA on `CourtDetailsScreen` (disabled when the venue has no rate) and "My bookings" in `ProfileScreen` (list + cancel). New `book-court`/`my-bookings` screens + `player.bookings.create` permission (synced to api/web). API change (sibling `api/`): new `settings` slice + `payments/checkout` (both on `/lists`). |
| 2026-06-02 | Nearby tab: **filter chips + filter sheet now narrow the list.** Lifted filter state into `NearbyScreen` (new `venueFilters.ts` — `VenueFilters` model, `makeDefaultFilters`, `matchesFilters` predicate, `countActiveFilters`); `NearbyFilterSheet` is now controlled (court type, price Any/Free/Paid, open play, distance cap, amenities) and the quick chips (Games here / Indoor / Free / Lighted) edit the same state. Filtering or locating switches the list to the full venue set (client-side filter — so a filter can't hide matches on unfetched pages) and hides "Load more"; otherwise the server-paged directory is unchanged. The Filter button shows an active-filter count. Amenity options pruned to flags that carry data (dropped Restrooms/Pro shop — `true` for no seeded venue). No API change. |
| 2026-06-02 | **Nearby** tab (renamed from "Courts" in `TabBar`/`Sidebar`): added **distance-based search**. A "Near me" chip + the floating locate button request the user's location (new `shared/lib/geo.ts` — haversine + distance formatting + a geolocation wrapper) and show the courts **near them** — only locatable venues, ranked nearest-first and limited to a radius (default 25 mi, adjustable via the filter sheet slider), not the whole directory (`resolveNearby` in `NearbyScreen`). Falls back to the nearest few if nothing is in range so the list never blanks. Each court shows its distance on rows/popups; the map frames the user (a "you are here" dot) + the nearby pins. Search keeps working alongside. **Open to guests** (it's a browse aid, like the rest of the tab); the new `player.venues.locate` permission (added to all three synced copies + the API `PERMISSION_CATALOGUE`/role defaults) only governs signed-in users, via `canLocate = !isLoggedIn \|\| userHasPermission(...)`. No API route change (no `/lists` edit). |
| 2026-06-01 | Wired **profile editing** + **onboarding memory** to the account. `EditProfileScreen` save now persists to `PATCH /me` via a new `authStore.updateProfile()`; added `updateMe`/`ProfileUpdate` to `api.ts` and `duprForTier` to `skillTiers.ts`. Onboarding completion is remembered on the account: new `hasOnboarded` flag on the user (API `auth.model`/`auth.controller` + `/me` payload), `authStore.completeOnboarding()`, and `App.tsx` now derives the onboarding gate from `user.hasOnboarded` instead of in-memory state. Both flows stay behind the existing `player.profile.manage` permission; `PATCH /me` remains `requireAuth` (self-scoped). API route surface unchanged (no `/lists` edit). |
| 2026-05-29 | Courts tab: added cursor **pagination** (20/page, "Load more") and venue **images** on list cards. `listVenues` now returns `{ items, cursor }` and accepts a `cursor` param; `ApiVenue` gained `image`. API change (sibling `api/`): `listVenues` controller attaches a media-derived `image` per venue via one batched `Media` lookup (mirrors the get-by-id `image`). Cards render the image with a gradient fallback. |
| 2026-05-29 | Wired the "Courts" tab to the live API. `NearbyScreen` lists venues from `GET /api/v1/venues` and `CourtDetailsScreen` loads a venue via `GET /api/v1/venues/:id` (by slug or `_id`). Added `listVenues`/`getVenue` + `ApiVenue`/`ApiVenueDetail` to `api.ts` and a `venueDisplay.ts` formatter module (price/location/tags/amenities/mapsUrl). Both screens fetch on mount with real loading/error/empty(not-found) states; detail screens remount per id via `key`. Real data is sparse, so all fields degrade gracefully. Games-on-venue section remains demo. |
| 2026-05-29 | Wired login to the live API and made the current-user UI dynamic. Added `shared/lib/api.ts` (auth client + token storage) and `shared/lib/authStore.ts` (Zustand auth store — first real use of the Zustand dep). `LoginScreen` now calls `useAuthStore().login()` → `POST /api/v1/auth/login`; `App.tsx` restores the session via `/me` on mount. The greeting, profile page, edit-profile prefill, sidebar, and nearby header now read the user from the store (no prop-threading) instead of the hardcoded "Riley Pickler". Profile stats remain demo data; edit-profile save not yet wired to `PATCH /me`. Extracted `getInitials` to `shared/lib/initials.ts`. |
| 2026-05-28 | Restructured `app/src/` into feature-based vertical slices (`features/{auth,home,games,venues,clubs,profile,search}` + `shared/{components,hooks,lib,styles}`), mirroring the `web/` convention. Pure file-move + import-update refactor — no behavior changes. Filter sheets co-located with their owning feature (`GameFilterSheet` → `features/games/`, `NearbyFilterSheet` → `features/venues/`). |
| 2026-05-27 | Commit `c4ceec6` — Removed `TopBar`, `Sidebar`, and `FAB` from `components/layout/`. Create action moved into `TabBar.onCreate`. Added `CourtIllustration`, `GameRow`, `Segmented`, `Toast` UI primitives. Polished all major screens. |
| 2026-05-27 | Commit `0e32861` — Added `LandingScreen` as new cold-start entry. Added `components/filters/` (bottom sheets replacing routed filter screens), `components/forms/` (FormField/Select/TierPicker), `hooks/` (useForm, usePrefersReducedMotion, useTheme), `lib/demoState.tsx`, `lib/skillTiers.ts`. Replaced `LoadingSpinner` with `LoadingSkeleton`. Added `BottomSheet`, `DuprExplainerSheet`, `OfflineBanner`, `DemoStateControl`. Included `Redesign/` reference assets. |
| 2026-05-28 | Removed the stale `docs/pickleplay-*` inventory (markdown, CSVs, xlsx). They were written against the pre-migration `src/screens/`-`src/components/` layout and the old "PicklePlay" brand, and had drifted far enough to mislead. Future inventory should be regenerated from current code rather than maintained by hand. |
| 2026-05-26 | Commit `16acf6c` — Added PWA install/update support (`pwaUpdate.ts`, install prompt, OpenStreetMap tile runtime caching) and PWA asset icons. |
